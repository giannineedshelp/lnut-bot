# LanguageNut Bot Updates

## Latest Fixes (May 2026)
- **Fake Accuracy Now Works**: Set your accuracy range in settings (e.g., 85-92%). The bot randomly picks how many answers are \"correct\" per task to look natural. Toggle stealth off for 100% perfect scores.
- **Fake Time Fixed**: Per-question time range now controls how long each vocab takes (e.g., 5-8 seconds). Total time looks human-like with jitter.
- **XP Per Task Setting**: Customize XP earned per vocab (default 200). Edit in settings panel.
- **Stealth Toggle**: Turn stealth on/off. Off = perfect/fast, On = realistic human mistakes + timing.
- **Fixed Incorrect Vocabs**: Now sends actual incorrect ones (was empty before).
- **Speed Setting**: Controls delays between tasks (3-3600s).
- **No More Duplicates**: Removed duplicate files/commands.
- **Better Settings Panel**: Added XP button + display.
- **Syntax Fixes**: Fixed indentation bugs so bot starts cleanly.

## Previous Updates
- Added leaderboard with class/school/global views + XP needed for target rank.
- Graceful error handling (no more crash loops).
- Guild-only commands (no DM spam).
- Encrypted login credentials.

**All changes tested. Bot is ready to grind XP safely!** 🎮
